# A Study on Session Recommendation Model Based on Multi-Feature and Knowledge Graph 

Session Recommendation model based on Multi-Feature，MFSR

# install
```bash
pip install -r requirements.txt
```

# Usage

```bash
cd pytorch_code
python main.py
```